/**
 * Standard_SedecoRedondeo
 * RequireJS configuration — mapea el alias del componente JS.
 */
var config = {
    map: {
        '*': {
            'Standard_SedecoRedondeo/js/view/checkout/summary/sedeco-rounding':
                'Standard_SedecoRedondeo/js/view/checkout/summary/sedeco-rounding'
        }
    }
};
